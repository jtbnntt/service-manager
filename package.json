{
  "name": "@jtbnntt/appconstructor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf ./dist",
    "copy-package-json": "cp package.json ./dist/",
    "dist": "npm run prepare-dist && npm publish ./dist",
    "prepare-dist": "npm run clean && npm run build && npm run copy-package-json",
    "tarball": "npm run prepare-dist && cd dist && TARBALL=$(npm pack) && echo $TARBALL && mv $TARBALL ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.7.2",
    "eslint": "^6.2.1",
    "typescript": "^3.5.3"
  }
}
